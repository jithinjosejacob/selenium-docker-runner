**Run tests in Jenkins CI**

https://github.com/jithinjosejacob/selenium-docker repo has already pushed the base image to docker hub.

docker-compose file will set up a grid and run the tests

Jenkinsfile contains instructions to run the docker-compose file in CI
